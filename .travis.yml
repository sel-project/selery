language: d

d:
 - dmd-2.073.2
 - ldc-1.2.0

script:
 - cd build
 - dub init.d --compiler=$DC --build=release
 - dub build --single --compiler=$DC --build=release hub.d
 - dub build --single --compiler=$DC --build=release node.d
 - dub build --single --compiler=$DC --build=release lite.d

notifications:
 - email: false
